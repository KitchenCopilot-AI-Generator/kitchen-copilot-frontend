# TODO List

1. Containerize the application.
    - Create a Dockerfile for the application.
    - Build and test the Docker image locally.
    - Push the Docker image to Azure Container Registry.

2. Host the frontend as an Azure Static Web App.
    - Configure the frontend for deployment.
    - Set up an Azure Static Web App resource.
    - Deploy the frontend to Azure.

3. Add allergen configurations.
    - Define a schema for allergen data.
    - Implement backend support for allergen configurations.
    - Update the frontend to display and manage allergen information.